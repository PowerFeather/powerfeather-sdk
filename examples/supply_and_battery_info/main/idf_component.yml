version: "1.0.0"
description: "PowerFeather SDK Supply and Battery Info Example"
dependencies:
  powerfeather/powerfeather-sdk:
    version: "^0.9.0"
    override_path: "../../../" # three levels up, pointing the directory with the component itself